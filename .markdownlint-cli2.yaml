config:
  MD013:
    line_length: 120
  MD033:
    allowed_elements:
      - "details"
      - "summary"

ignores:
  # Legal text — do not reformat
  - "LICENSE.md"
  # Claude Code command files are prompt templates, not documentation
  - ".claude/commands/**"
