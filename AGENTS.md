# AI Agent Instructions

This repo holds default community health files for all `benhigham` repositories. GitHub serves them to any
repository that lacks its own version, so edits here change what visitors see across every repo — keep the
wording repo-agnostic ("this project", not a specific repo).

There is no build, lint or CI. Commits use Conventional Commits (`docs:`, `chore:`, etc.).
