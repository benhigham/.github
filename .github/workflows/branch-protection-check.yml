# Validates branch protection settings weekly and creates issues if configuration problems are found.
# Checks for required settings like PR reviews, status checks, and other security configurations.
name: Branch Protection Check

on:
  schedule:
    - cron: "0 0 * * 1" # Weekly on Monday
  workflow_dispatch:
  workflow_call:
    inputs:
      branch:
        description: 'Branch to check protection for'
        type: string
        default: 'main'
        required: false

permissions:
  contents: read
  issues: write

concurrency:
  group: branch-protection-check
  cancel-in-progress: true

jobs:
  check:
    name: Check Branch Protection
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check branch protection rules
        uses: actions/github-script@v7
        env:
          INPUT_BRANCH: ${{ inputs.branch }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const branch = process.env.INPUT_BRANCH || 'main';

            async function findOrCreateIssue(titleFragment, issueBody, labels) {
              const { data: existingIssues } = await github.rest.issues.listForRepo({
                owner,
                repo,
                state: 'open',
                labels: labels,
                per_page: 10
              });

              const existingIssue = existingIssues.find(issue =>
                issue.title.includes(titleFragment)
              );

              if (existingIssue) {
                await github.rest.issues.createComment({
                  owner,
                  repo,
                  issue_number: existingIssue.number,
                  body: `## 🔄 Updated Check Results\n\n${issueBody}`
                });
                console.log(`Updated existing issue #${existingIssue.number}`);
              } else {
                await github.rest.issues.create({
                  owner,
                  repo,
                  title: `${titleFragment} for ${branch}`,
                  body: issueBody,
                  labels: labels
                });
                console.log('Created new issue');
              }
            }

            try {
              const { data: protection } = await github.rest.repos.getBranchProtection({
                owner,
                repo,
                branch: branch
              });

              console.log('✅ Branch protection is enabled for', branch);
              console.log('Protection rules:', JSON.stringify(protection, null, 2));

              const issues = [];

              if (!protection.required_pull_request_reviews) {
                issues.push('❌ Pull request reviews are not required');
              }

              if (!protection.enforce_admins?.enabled) {
                issues.push('⚠️  Rules do not apply to administrators');
              }

              if (!protection.required_status_checks) {
                issues.push('⚠️  No required status checks configured');
              }

              if (issues.length > 0) {
                console.log('\n⚠️  Issues found:');
                issues.forEach(issue => console.log(issue));

                const issueBody = [
                  `## Branch Protection Check Results\n`,
                  `The following issues were found with branch protection for \`${branch}\`:\n`,
                  issues.map(i => `- ${i}`).join('\n'),
                  `\n**Recommendations:**`,
                  `- Enable pull request reviews`,
                  `- Apply rules to administrators`,
                  `- Configure required status checks`,
                  `\n---`,
                  `*Automated check run on ${new Date().toISOString()}*`,
                ].join('\n');

                try {
                  await findOrCreateIssue('Branch Protection Configuration Issues', issueBody, ['security', 'branch-protection', 'chore']);
                } catch (issueError) {
                  console.error(`Failed to create/update issue: ${issueError.message}`);
                  core.setFailed(`Branch protection issues found for ${branch} AND issue creation failed: ${issueError.message}`);
                }
              } else {
                console.log('\n✅ All branch protection checks passed!');
              }

            } catch (error) {
              if (error.status === 404) {
                console.error(`❌ No branch protection found for ${branch}`);

                const issueBody = [
                  `## ⚠️  Branch Protection Missing\n`,
                  `Branch protection is not configured for \`${branch}\`.\n`,
                  `**Recommended settings:**`,
                  `- Require pull request reviews`,
                  `- Require status checks to pass`,
                  `- Enforce rules for administrators`,
                  `- Require linear history`,
                  `\n---`,
                  `*Automated check run on ${new Date().toISOString()}*`,
                ].join('\n');

                try {
                  await findOrCreateIssue('Branch Protection Not Configured', issueBody, ['security', 'branch-protection', 'priority: high']);
                } catch (issueError) {
                  console.error(`Failed to create/update issue: ${issueError.message}`);
                  console.error('Branch protection finding:', issueBody);
                  core.setFailed(`Branch protection is missing for ${branch} AND issue creation failed: ${issueError.message}`);
                }
              } else {
                throw error;
              }
            }
