name: "Setup Terraform toolchain"
description: "Installs tools defined in the repo's .mise.toml (typically Terraform, Terragrunt, tflint, etc.)."

runs:
  using: composite
  steps:
    - name: Install Terraform toolchain via mise
      uses: jdx/mise-action@1648a7812b9aeae629881980618f079932869151 # v4
